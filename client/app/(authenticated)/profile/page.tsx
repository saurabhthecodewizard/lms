"use client"
import React from 'react';
import Protected from '@/hooks/useProtected';

const Page: React.FC = () => {
    return (
        <main className='w-full h-screen'>
            PROFILE
        </main>
    )
};

export default Page;