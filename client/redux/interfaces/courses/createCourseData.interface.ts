import CreateLink from "./createLink.interface";

interface CreateCourseData {
    title: string;
    description: string;
    videoUrl: string;
    videoLength: number;
}

export default CreateCourseData;